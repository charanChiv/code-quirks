def SOE(n):
    nums = [True for _ in range(2,n)]

    for i in range(2, int(n**0.5) + 1):
        if nums[i]:
            for j in range(i * i, n+1, i):
                nums[j] = False
    return nums

print(SOE(10))
